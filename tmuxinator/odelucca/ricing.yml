name: odelucca-ricing

windows:
- dotfiles:
    layout: main-vertical
    root: <%= ENV["CODEDIR"] %>/odelucca/dotfiles
    panes:
    - clear
    - cleartree

- ricer:
    layout: main-vertical
    root: <%= ENV["CODEDIR"] %>/odelucca/ricer
    panes:
    - clear
    - cleartree
