# Updates Fresh
fresh freshshell/fresh bin/fresh --bin=~/.local/bin/fresh

# ZSH
fresh config/zshrc.min --file=~/.zshrc
fresh config/zshenv.min --file=~/.zshenv

# Shell
fresh shell/aliases/\*
fresh shell/functions/\*
